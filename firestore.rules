rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ログイン済みユーザーのみ読み書き可能（Microsoft SSO 経由）
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
